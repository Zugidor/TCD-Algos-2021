public class Node {
	// Variables needed to initialize a Node object.
	double dist;
	int vertex;

	// Constructor for a Node object, with the distance and vertex as parameters.
	Node(double pDist, int pVertex)
	{
		dist = pDist;
		vertex = pVertex;
	}
}